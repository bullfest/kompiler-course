name := "punkt0"

version := "1.0"

// First, make sure you have the Java 8 JDK installed
// And do not change the scala version
scalaVersion := "2.12.6"

scalacOptions ++= Seq("-deprecation", "-unchecked")
